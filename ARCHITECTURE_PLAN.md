# 🏗️ План архитектуры и функционала проекта Klust

## 📋 Общая архитектура

Проект состоит из **2.5 интерфейсов**:
1. **Админка** (`/admin/*`) - для КубГТУ (роли: `admin`, `teacher`)
2. **Клиент Студента** (`/student/*`) - роль: `student`
3. **Клиент Партнера** (`/partner/*`) - роль: `partner`

---

## 🔧 Шаг 3: Структура Vue компонентов и страниц

### 3.1 Layouts (Лейауты)

#### `resources/js/Layouts/AdminLayout.vue`
**Назначение:** Основной лейаут админ-панели КубГТУ

**Компоненты:**
- **Sidebar** (левое меню):
  - Dashboard
  - Пользователи (студенты, партнеры, преподаватели)
  - Кейсы (все кейсы, создание, редактирование)
  - Навыки (Skills) - управление
  - Бейджи (Badges) - управление
  - Симуляторы - управление
  - Статистика и аналитика
  - Настройки системы

- **Header** (верхняя панель):
  - Логотип КубГТУ
  - Поиск по системе
  - Уведомления
  - Профиль пользователя (dropdown: профиль, настройки, выход)

- **Main Content Area** - область для контента страниц

**Стилизация:**
- Темная/светлая тема (опционально)
- Цвета КубГТУ (уточнить брендбук)
- Адаптивность для планшетов

---

#### `resources/js/Layouts/ClientLayout.vue`
**Назначение:** Базовый лейаут для клиентской части (общие элементы)

**Общие компоненты:**
- **Header:**
  - Логотип Klust
  - Главное меню (зависит от роли)
  - Кнопка профиля
  - Уведомления

- **Footer** (общий для всех):
  - Контакты
  - Соцсети
  - Полезные ссылки

---

#### `resources/js/Layouts/StudentLayout.vue`
**Наследует:** `ClientLayout.vue`

**Дополнительные элементы:**
- **Sidebar** (левое меню студента):
  - Dashboard (личная панель)
  - Мои кейсы (текущие, завершенные, архив)
  - Доступные кейсы (каталог для подачи заявки)
  - Мой профиль
  - Навыки и достижения (Skills & Badges)
  - Симуляторы
  - Прогресс обучения

- **Индикаторы:**
  - Общий уровень/рейтинг
  - Количество очков
  - Текущие активные кейсы

---

#### `resources/js/Layouts/PartnerLayout.vue`
**Наследует:** `ClientLayout.vue`

**Дополнительные элементы:**
- **Sidebar** (левое меню партнера):
  - Dashboard (панель партнера)
  - Мои кейсы (созданные, активные, завершенные)
  - Создать новый кейс
  - Команды (управление командами студентов на кейсах)
  - Профиль компании
  - Аналитика по кейсам

- **Индикаторы:**
  - Количество активных кейсов
  - Количество команд
  - Статистика по завершенным кейсам

---

#### `resources/js/Layouts/GuestLayout.vue`
**Назначение:** Лейаут для неавторизованных пользователей

**Компоненты:**
- Минималистичный header с логотипом
- Кнопки "Войти" и "Регистрация"
- Основной контент (landing page, формы входа/регистрации)

---

### 3.2 Страницы (Pages)

#### 📁 `resources/js/Pages/Auth/`

##### `Login.vue`
**Функционал:**
- Форма входа (email/логин + пароль)
- Проверка роли после входа и редирект на соответствующий dashboard
- Ссылка "Забыли пароль?"
- Ссылка на регистрацию (для студентов)

**Валидация:**
- Email/логин обязателен
- Пароль обязателен
- Показ ошибок от сервера

---

##### `Register.vue`
**Функционал:**
- Выбор типа регистрации (студент / партнер)
- Форма для студента:
  - kubgtu_id (ID КубГТУ)
  - ФИО
  - Email
  - Пароль + подтверждение
  - Курс (выбор)
  
- Форма для партнера:
  - Название компании
  - Контактное лицо (ФИО)
  - Email
  - Пароль + подтверждение
  - Телефон
  - Описание компании (textarea)

**Валидация:**
- Все поля обязательны
- Email уникален
- Пароли совпадают
- kubgtu_id уникален для студентов

---

#### 📁 `resources/js/Pages/Admin/`

##### `Dashboard.vue`
**Функционал:**
- **Виджеты статистики:**
  - Общее количество студентов
  - Общее количество партнеров
  - Активные кейсы
  - Завершенные кейсы (сегодня/неделя/месяц)
  - Новые регистрации (сегодня/неделя)
  
- **Графики:**
  - Динамика регистраций (студенты/партнеры) за период
  - Статистика по кейсам (созданные/активные/завершенные)
  - Топ-5 навыков по популярности
  - Распределение студентов по курсам

- **Последние активности:**
  - Новые регистрации
  - Последние созданные кейсы
  - Последние завершенные кейсы

- **Быстрые действия:**
  - Создать кейс
  - Добавить навык
  - Добавить бейдж
  - Просмотр всех пользователей

---

##### `Users/Index.vue`
**Функционал:**
- Таблица всех пользователей с фильтрами:
  - По роли (student/partner/teacher/admin)
  - По статусу (активен/заблокирован)
  - Поиск по имени, email, kubgtu_id
  
- Колонки таблицы:
  - ID
  - ФИО
  - Email
  - kubgtu_id (для студентов)
  - Роль
  - Курс (для студентов)
  - Дата регистрации
  - Статус
  - Действия (просмотр/редактирование/удаление)

- Пагинация
- Массовые действия (блокировка/разблокировка/удаление)

---

##### `Users/Show.vue`
**Функционал:**
- Полная информация о пользователе
- Редактирование профиля (модальное окно или отдельная страница)
- История активности
- Прикрепленные профили (StudentProfile/PartnerProfile/TeacherProfile)
- Навыки и бейджи пользователя
- История кейсов пользователя

---

##### `Users/Create.vue` или модальное окно
**Функционал:**
- Создание пользователя администратором
- Выбор роли
- Заполнение основных данных
- Автоматическое создание соответствующего профиля

---

##### `Cases/Index.vue`
**Функционал:**
- Таблица всех кейсов с фильтрами:
  - По статусу (черновик/активен/завершен/архив)
  - По партнеру (создателю)
  - По дате создания
  - Поиск по названию/описанию
  
- Колонки:
  - ID
  - Название
  - Партнер (создатель)
  - Статус
  - Количество заявок
  - Активные команды
  - Дата создания
  - Действия

---

##### `Cases/Create.vue`
**Функционал:**
- Форма создания кейса (для админа от имени партнера):
  - Выбор партнера (если создается от имени партнера)
  - Название кейса
  - Описание (rich text editor)
  - Требования к команде (количество человек)
  - Навыки, необходимые для кейса (multi-select)
  - Дедлайн
  - Прикрепление файлов (если нужно)
  - Статус (черновик/активен)

---

##### `Cases/Show.vue`
**Функционал:**
- Полная информация о кейсе
- Список заявок на кейс
- Управление заявками (одобрение/отклонение)
- Просмотр команд
- Редактирование кейса
- Архивация/удаление

---

##### `Skills/Index.vue`
**Функционал:**
- Таблица всех навыков
- Создание/редактирование/удаление навыков
- Фильтры по категориям (если есть)
- Поиск по названию

---

##### `Badges/Index.vue`
**Функционал:**
- Таблица всех бейджей
- Создание/редактирование/удаление бейджей
- Загрузка изображения бейджа
- Условия получения бейджа

---

##### `Simulators/Index.vue`
**Функционал:**
- Управление симуляторами
- Создание/редактирование/удаление
- Просмотр статистики использования

---

#### 📁 `resources/js/Pages/Client/Student/`

##### `Dashboard.vue`
**Функционал:**
- **Личная статистика:**
  - Уровень/рейтинг студента
  - Общее количество очков
  - Количество завершенных кейсов
  - Количество полученных бейджей
  - Прогресс по навыкам (график/диаграмма)
  
- **Активные кейсы:**
  - Карточки текущих кейсов с прогрессом
  - Ближайшие дедлайны
  
- **Рекомендации:**
  - Рекомендуемые кейсы (на основе навыков)
  - Новые кейсы в каталоге
  
- **Последние достижения:**
  - Полученные бейджи
  - Новые навыки
  - Завершенные кейсы

---

##### `Cases/Index.vue` - Каталог кейсов
**Функционал:**
- Фильтры:
  - По навыкам (multi-select)
  - По партнеру
  - По статусу (доступные/закрытые)
  - По сложности (если есть)
  
- Карточки кейсов:
  - Название
  - Партнер
  - Требуемые навыки
  - Описание (краткое)
  - Количество заявок
  - Дедлайн
  - Кнопка "Подать заявку" (если доступно)
  
- Поиск по названию/описанию
- Пагинация

---

##### `Cases/Show.vue` - Детали кейса
**Функционал:**
- Полное описание кейса
- Требования
- Необходимые навыки
- Информация о партнере
- Статус заявки студента (если подавал)
- Кнопка "Подать заявку" / "Отозвать заявку"
- История изменений статуса заявки

---

##### `Cases/MyCases.vue` - Мои кейсы
**Функционал:**
- Вкладки:
  - Текущие (активные кейсы в команде)
  - Заявки (pending approval)
  - Завершенные
  - Отклоненные
  
- Карточки кейсов с:
  - Прогресс выполнения
  - Информация о команде
  - Роль в команде (лидер/участник)
  - Дедлайн
  - Статус

---

##### `Cases/Team.vue` - Команда на кейсе
**Функционал:**
- Информация о команде
- Список участников с ролями
- Общий прогресс команды
- Чат/обсуждения (если реализовано)
- Задачи команды

---

##### `Profile/Index.vue` - Профиль студента
**Функционал:**
- Редактирование личных данных
- Загрузка аватара
- Изменение пароля
- Просмотр kubgtu_id (readonly)

---

##### `Skills/Index.vue` - Навыки студента
**Функционал:**
- Список всех навыков с уровнями
- Прогресс по каждому навыку
- История получения очков по навыкам
- Фильтры по категориям

---

##### `Badges/Index.vue` - Бейджи студента
**Функционал:**
- Коллекция полученных бейджей
- Условия получения бейджей
- Прогресс к следующим бейджам

---

##### `Simulators/Index.vue` - Доступные симуляторы
**Функционал:**
- Список симуляторов
- Запуск симулятора
- История сессий
- Результаты прохождения

---

#### 📁 `resources/js/Pages/Client/Partner/`

##### `Dashboard.vue`
**Функционал:**
- **Статистика компании:**
  - Количество созданных кейсов
  - Активные кейсы
  - Завершенные кейсы
  - Количество команд
  - Средняя оценка кейсов (если есть рейтинг)
  
- **Графики:**
  - Динамика создания кейсов
  - Популярность кейсов (по заявкам)
  - Процент завершенных кейсов
  
- **Активные кейсы:**
  - Карточки с прогрессом команд
  - Ближайшие дедлайны
  
- **Последние активности:**
  - Новые заявки на кейсы
  - Завершенные кейсы
  - Новые команды

---

##### `Cases/Index.vue` - Мои кейсы (партнера)
**Функционал:**
- Вкладки:
  - Все кейсы
  - Черновики
  - Активные
  - Завершенные
  - Архив
  
- Фильтры:
  - По статусу
  - По дате создания
  - Поиск по названию
  
- Карточки/таблица кейсов:
  - Название
  - Статус
  - Количество заявок
  - Количество активных команд
  - Дедлайн
  - Действия (редактировать/просмотр/архивировать)

---

##### `Cases/Create.vue` - Создание кейса
**Функционал:**
- Форма создания:
  - Название (обязательно)
  - Описание (rich text editor)
  - Требования к команде:
    - Количество участников
    - Требуемые навыки (multi-select)
    - Роли в команде (опционально)
  - Дедлайн
  - Статус (черновик/активен)
  - Прикрепление файлов/ресурсов
  
- Валидация всех полей
- Предпросмотр перед публикацией

---

##### `Cases/Show.vue` - Детали кейса партнера
**Функционал:**
- Полная информация о кейсе
- Редактирование кейса
- **Заявки на кейс:**
  - Таблица всех заявок
  - Фильтры по статусу
  - Просмотр заявки (информация о студенте/команде)
  - Одобрение/отклонение заявок
  - Комментарии к заявкам
  
- **Команды:**
  - Список команд, работающих над кейсом
  - Информация о команде
  - Прогресс команды
  - Взаимодействие с командой (сообщения, если реализовано)
  
- Статистика по кейсу

---

##### `Cases/Edit.vue` - Редактирование кейса
**Функционал:**
- Те же поля, что и в Create
- Возможность изменения статуса
- Архивация кейса

---

##### `Teams/Index.vue` - Команды партнера
**Функционал:**
- Список всех команд на кейсах партнера
- Фильтры:
  - По кейсу
  - По статусу команды
  - По дате создания
  
- Информация о командах:
  - Название кейса
  - Состав команды
  - Роль лидера
  - Прогресс
  - Статус

---

##### `Teams/Show.vue` - Детали команды
**Функционал:**
- Полная информация о команде
- Состав с ролями
- Прогресс выполнения кейса
- История активности команды
- Взаимодействие с командой

---

##### `Profile/Index.vue` - Профиль партнера
**Функционал:**
- Редактирование данных компании:
  - Название компании
  - Описание
  - Логотип компании
  - Контакты (телефон, email, сайт)
  - Адрес
  - Социальные сети (опционально)
  
- Изменение пароля
- Настройки уведомлений

---

##### `Analytics/Index.vue` - Аналитика партнера
**Функционал:**
- Общая статистика по всем кейсам
- Графики:
  - Популярность кейсов
  - Конверсия заявок в команды
  - Среднее время выполнения кейса
  - Рейтинг студентов (топ участников)
  
- Экспорт данных (CSV/Excel)

---

### 3.3 Компоненты (Components)

#### 📁 `resources/js/Components/Shared/`

##### `FlashMessage.vue`
**Функционал:**
- Отображение flash сообщений (success/error/info)
- Автоматическое скрытие через 5 секунд
- Кнопка закрытия вручную
- Разные стили для типов сообщений

---

##### `NotificationBell.vue`
**Функционал:**
- Иконка колокольчика в header
- Счетчик непрочитанных уведомлений
- Dropdown с последними уведомлениями
- Отметка как прочитанное
- Переход к полному списку уведомлений

---

##### `UserAvatar.vue`
**Функционал:**
- Отображение аватара пользователя
- Fallback на инициалы, если нет фото
- Разные размеры (sm, md, lg)
- Ссылка на профиль (опционально)

---

##### `Breadcrumbs.vue`
**Функционал:**
- Навигационные крошки
- Автоматическая генерация из route
- Ручная настройка при необходимости

---

##### `LoadingSpinner.vue`
**Функционал:**
- Спиннер для загрузки
- Разные размеры
- Центрирование или inline

---

#### 📁 `resources/js/Components/UI/`

##### `Button.vue`
**Функционал:**
- Переиспользуемая кнопка
- Варианты: primary, secondary, danger, success
- Размеры: sm, md, lg
- Состояния: loading, disabled
- Иконки (опционально)

---

##### `Input.vue`
**Функционал:**
- Текстовое поле с валидацией
- Различные типы (text, email, password, number)
- Label и placeholder
- Ошибки валидации
- Иконки (left/right)

---

##### `Select.vue`
**Функционал:**
- Выпадающий список
- Single/Multiple выбор
- Поиск в опциях
- Группировка опций

---

##### `Modal.vue`
**Функционал:**
- Модальное окно
- Различные размеры
- Кнопки закрытия
- Слоты для контента
- Закрытие по клику вне модального окна

---

##### `Card.vue`
**Функционал:**
- Карточка контента
- Заголовок, тело, footer
- Варианты стилей

---

##### `Table.vue`
**Функционал:**
- Таблица данных
- Сортировка по колонкам
- Фильтрация
- Пагинация
- Выбор строк (checkbox)

---

##### `Badge.vue` (UI компонент, не модель)
**Функционал:**
- Отображение бейджа (изображение + название)
- Размеры
- Tooltip с описанием условий получения

---

##### `SkillCard.vue`
**Функционал:**
- Карточка навыка
- Название
- Уровень/прогресс
- Иконка
- Количество очков

---

##### `CaseCard.vue`
**Функционал:**
- Карточка кейса
- Название, описание (краткое)
- Партнер
- Требуемые навыки (теги)
- Статус
- Действия (кнопки)

---

##### `TeamCard.vue`
**Функционал:**
- Карточка команды
- Название/кейс
- Участники (аватары)
- Прогресс
- Статус

---

##### `ProgressBar.vue`
**Функционал:**
- Прогресс-бар
- Процент выполнения
- Цвета по статусу
- Анимация

---

##### `StatsWidget.vue`
**Функционал:**
- Виджет статистики для dashboard
- Заголовок
- Значение (число)
- Иконка
- Тренд (↑↓) опционально
- Ссылка на детальную страницу

---

---

## 🎮 Шаг 4: Контроллеры Laravel

### 4.1 Аутентификация

#### 📁 `app/Http/Controllers/Auth/`

##### `LoginController.php`
**Методы:**
- `show()` - возвращает `Inertia::render('Auth/Login')`
- `login(Request $request)` - обработка логина:
  - Валидация email/пароль
  - Попытка авторизации
  - Редирект на соответствующий dashboard по роли
  - Возврат ошибок при неудаче

---

##### `RegisterController.php`
**Методы:**
- `show()` - возвращает `Inertia::render('Auth/Register')`
- `register(Request $request)` - обработка регистрации:
  - Валидация данных
  - Создание пользователя
  - Создание соответствующего профиля (StudentProfile/PartnerProfile)
  - Назначение роли
  - Автоматический login или редирект на логин
  - Отправка welcome email (опционально)

---

##### `LogoutController.php` или метод в LoginController
**Методы:**
- `logout()` - выход пользователя и редирект на главную

---

### 4.2 Админка

#### 📁 `app/Http/Controllers/Admin/`

##### `DashboardController.php`
**Методы:**
- `index()` - главная страница админки:
  - Подсчет статистики (студенты, партнеры, кейсы)
  - Последние активности
  - Возвращает `Inertia::render('Admin/Dashboard', [статистика])`

**Middleware:** `auth`, `role:admin|teacher`

---

##### `UsersController.php`
**Методы:**
- `index(Request $request)` - список пользователей:
  - Фильтрация по ролям, статусу
  - Поиск
  - Пагинация
  - Возвращает `Inertia::render('Admin/Users/Index', [пользователи, фильтры])`

- `show(User $user)` - детали пользователя:
  - Загрузка всех связей (профили, навыки, бейджи, кейсы)
  - Возвращает `Inertia::render('Admin/Users/Show', [user])`

- `create()` - форма создания пользователя
- `store(Request $request)` - сохранение нового пользователя
- `edit(User $user)` - форма редактирования
- `update(Request $request, User $user)` - обновление
- `destroy(User $user)` - удаление (мягкое удаление)

**Middleware:** `auth`, `role:admin` (только admin может удалять)

---

##### `CasesController.php`
**Методы:**
- `index(Request $request)` - список всех кейсов:
  - Фильтрация по статусу, партнеру
  - Поиск
  - Пагинация
  - Возвращает `Inertia::render('Admin/Cases/Index', [кейсы])`

- `show(CaseModel $case)` - детали кейса:
  - Заявки, команды, статистика
  - Возвращает `Inertia::render('Admin/Cases/Show', [case])`

- `create()` - форма создания кейса
- `store(Request $request)` - сохранение
- `edit(CaseModel $case)` - форма редактирования
- `update(Request $request, CaseModel $case)` - обновление
- `destroy(CaseModel $case)` - удаление

**Middleware:** `auth`, `role:admin|teacher`

---

##### `SkillsController.php`
**Методы:**
- `index()` - список всех навыков
- `store(Request $request)` - создание навыка
- `update(Request $request, Skill $skill)` - обновление
- `destroy(Skill $skill)` - удаление

**Middleware:** `auth`, `role:admin`

---

##### `BadgesController.php`
**Методы:**
- `index()` - список всех бейджей
- `store(Request $request)` - создание бейджа (с загрузкой изображения)
- `update(Request $request, Badge $badge)` - обновление
- `destroy(Badge $badge)` - удаление

**Middleware:** `auth`, `role:admin`

---

##### `SimulatorsController.php`
**Методы:**
- `index()` - список симуляторов
- `store(Request $request)` - создание
- `update(Request $request, Simulator $simulator)` - обновление
- `destroy(Simulator $simulator)` - удаление

**Middleware:** `auth`, `role:admin`

---

### 4.3 Клиент - Студент

#### 📁 `app/Http/Controllers/Client/Student/`

##### `DashboardController.php`
**Методы:**
- `index()` - панель студента:
  - Личная статистика (очки, кейсы, бейджи)
  - Активные кейсы
  - Рекомендации
  - Последние достижения
  - Возвращает `Inertia::render('Client/Student/Dashboard', [статистика])`

**Middleware:** `auth`, `role:student`

---

##### `CasesController.php`
**Методы:**
- `index(Request $request)` - каталог доступных кейсов:
  - Фильтрация по навыкам, партнеру
  - Поиск
  - Исключение кейсов, на которые уже подал заявку
  - Пагинация
  - Возвращает `Inertia::render('Client/Student/Cases/Index', [кейсы, фильтры])`

- `show(CaseModel $case)` - детали кейса:
  - Информация о кейсе
  - Статус заявки студента (если есть)
  - Возвращает `Inertia::render('Client/Student/Cases/Show', [case, applicationStatus])`

- `myCases()` - мои кейсы:
  - Текущие, заявки, завершенные, отклоненные
  - Возвращает `Inertia::render('Client/Student/Cases/MyCases', [кейсы по категориям])`

- `apply(Request $request, CaseModel $case)` - подача заявки на кейс:
  - Валидация (можно ли подать заявку)
  - Создание CaseApplication
  - Уведомление партнеру
  - Редирект обратно на страницу кейса

- `withdraw(CaseApplication $application)` - отзыв заявки:
  - Проверка прав (только свой)
  - Удаление заявки
  - Редирект

- `team(CaseApplication $application)` - страница команды:
  - Информация о команде
  - Участники
  - Прогресс
  - Возвращает `Inertia::render('Client/Student/Cases/Team', [team])`

**Middleware:** `auth`, `role:student`

---

##### `ProfileController.php`
**Методы:**
- `show()` - просмотр профиля
- `edit()` - форма редактирования
- `update(Request $request)` - обновление профиля:
  - Валидация
  - Обновление User и StudentProfile
  - Загрузка аватара (если есть)
  - Редирект с flash сообщением

**Middleware:** `auth`, `role:student`

---

##### `SkillsController.php`
**Методы:**
- `index()` - навыки студента:
  - Все навыки с уровнями и прогрессом
  - История получения очков
  - Возвращает `Inertia::render('Client/Student/Skills/Index', [skills, history])`

**Middleware:** `auth`, `role:student`

---

##### `BadgesController.php`
**Методы:**
- `index()` - бейджи студента:
  - Полученные бейджи
  - Условия получения следующих
  - Возвращает `Inertia::render('Client/Student/Badges/Index', [badges])`

**Middleware:** `auth`, `role:student`

---

##### `SimulatorsController.php`
**Методы:**
- `index()` - список доступных симуляторов
- `start(Simulator $simulator)` - запуск симулятора (создание сессии)
- `session(SimulatorSession $session)` - продолжение сессии
- `complete(Request $request, SimulatorSession $session)` - завершение и сохранение результатов:
  - Начисление очков по навыкам
  - Обновление прогресса

**Middleware:** `auth`, `role:student`

---

### 4.4 Клиент - Партнер

#### 📁 `app/Http/Controllers/Client/Partner/`

##### `DashboardController.php`
**Методы:**
- `index()` - панель партнера:
  - Статистика компании
  - Графики
  - Активные кейсы
  - Последние активности
  - Возвращает `Inertia::render('Client/Partner/Dashboard', [статистика])`

**Middleware:** `auth`, `role:partner`

---

##### `CasesController.php`
**Методы:**
- `index(Request $request)` - мои кейсы партнера:
  - Фильтрация по статусу
  - Поиск
  - Пагинация
  - Только кейсы текущего партнера
  - Возвращает `Inertia::render('Client/Partner/Cases/Index', [кейсы])`

- `create()` - форма создания кейса
- `store(Request $request)` - сохранение кейса:
  - Валидация
  - Создание CaseModel от имени текущего партнера
  - Уведомление студентам (если кейс активен)

- `show(CaseModel $case)` - детали кейса:
  - Проверка прав (только свой кейс)
  - Заявки на кейс
  - Команды
  - Статистика
  - Возвращает `Inertia::render('Client/Partner/Cases/Show', [case, applications, teams])`

- `edit(CaseModel $case)` - форма редактирования
- `update(Request $request, CaseModel $case)` - обновление
- `archive(CaseModel $case)` - архивация кейса

- `applications(CaseModel $case)` - управление заявками:
  - Список заявок
  - Одобрение/отклонение заявок
  - Комментарии к заявкам

**Middleware:** `auth`, `role:partner`

---

##### `TeamsController.php`
**Методы:**
- `index(Request $request)` - все команды партнера:
  - Фильтрация по кейсу, статусу
  - Возвращает `Inertia::render('Client/Partner/Teams/Index', [teams])`

- `show(CaseApplication $application)` - детали команды:
  - Информация о команде
  - Прогресс
  - Взаимодействие
  - Возвращает `Inertia::render('Client/Partner/Teams/Show', [team])`

**Middleware:** `auth`, `role:partner`

---

##### `ProfileController.php`
**Методы:**
- `show()` - профиль компании
- `edit()` - форма редактирования
- `update(Request $request)` - обновление:
  - Обновление User и PartnerProfile
  - Загрузка логотипа компании
  - Обновление Partner (если есть отдельная модель)

**Middleware:** `auth`, `role:partner`

---

##### `AnalyticsController.php`
**Методы:**
- `index(Request $request)` - аналитика:
  - Статистика по всем кейсам партнера
  - Графики
  - Экспорт данных
  - Возвращает `Inertia::render('Client/Partner/Analytics/Index', [analytics])`

**Middleware:** `auth`, `role:partner`

---

### 4.5 Form Requests (Валидация)

Создать отдельные Form Request классы для валидации:

- `app/Http/Requests/Auth/LoginRequest.php`
- `app/Http/Requests/Auth/RegisterRequest.php`
- `app/Http/Requests/Admin/StoreUserRequest.php`
- `app/Http/Requests/Admin/UpdateUserRequest.php`
- `app/Http/Requests/Cases/StoreCaseRequest.php`
- `app/Http/Requests/Cases/UpdateCaseRequest.php`
- `app/Http/Requests/Student/ApplyCaseRequest.php`
- `app/Http/Requests/Partner/StoreCaseRequest.php`
- И т.д.

---

## 🎨 Стилизация и UI

### TailwindCSS
- Использовать утилиты Tailwind для стилизации
- Создать кастомные цвета в `tailwind.config.js` (цвета КубГТУ)
- Responsive дизайн (mobile-first)

### PrimeVue
- Использовать для сложных компонентов:
  - DataTable (для таблиц)
  - Dialog (модальные окна)
  - Dropdown
  - Calendar (датапикеры)
  - FileUpload (загрузка файлов)
  - Chart (графики для dashboard)

---

## 🔐 Безопасность и Middleware

### Роутинг с защитой

```php
// routes/web.php

// Гость
Route::middleware('guest')->group(function () {
    Route::get('/login', [Auth\LoginController::class, 'show'])->name('login');
    Route::post('/login', [Auth\LoginController::class, 'login']);
    Route::get('/register', [Auth\RegisterController::class, 'show'])->name('register');
    Route::post('/register', [Auth\RegisterController::class, 'register']);
});

// Авторизованные
Route::middleware('auth')->group(function () {
    
    // Редирект на dashboard по роли
    Route::get('/dashboard', function () {
        $user = auth()->user();
        if ($user->hasRole(['admin', 'teacher'])) {
            return redirect()->route('admin.dashboard');
        } elseif ($user->hasRole('student')) {
            return redirect()->route('student.dashboard');
        } elseif ($user->hasRole('partner')) {
            return redirect()->route('partner.dashboard');
        }
        return redirect()->route('login');
    })->name('dashboard');
    
    // Админка
    Route::prefix('admin')->middleware('role:admin|teacher')->name('admin.')->group(function () {
        Route::get('/dashboard', [Admin\DashboardController::class, 'index'])->name('dashboard');
        Route::resource('users', Admin\UsersController::class);
        Route::resource('cases', Admin\CasesController::class);
        Route::resource('skills', Admin\SkillsController::class);
        Route::resource('badges', Admin\BadgesController::class);
        Route::resource('simulators', Admin\SimulatorsController::class);
    });
    
    // Студент
    Route::prefix('student')->middleware('role:student')->name('student.')->group(function () {
        Route::get('/dashboard', [Client\Student\DashboardController::class, 'index'])->name('dashboard');
        Route::get('/cases', [Client\Student\CasesController::class, 'index'])->name('cases.index');
        Route::get('/cases/my', [Client\Student\CasesController::class, 'myCases'])->name('cases.my');
        Route::get('/cases/{case}', [Client\Student\CasesController::class, 'show'])->name('cases.show');
        Route::post('/cases/{case}/apply', [Client\Student\CasesController::class, 'apply'])->name('cases.apply');
        Route::delete('/cases/applications/{application}', [Client\Student\CasesController::class, 'withdraw'])->name('cases.withdraw');
        Route::get('/cases/team/{application}', [Client\Student\CasesController::class, 'team'])->name('cases.team');
        Route::get('/profile', [Client\Student\ProfileController::class, 'show'])->name('profile.show');
        Route::put('/profile', [Client\Student\ProfileController::class, 'update'])->name('profile.update');
        Route::get('/skills', [Client\Student\SkillsController::class, 'index'])->name('skills.index');
        Route::get('/badges', [Client\Student\BadgesController::class, 'index'])->name('badges.index');
        Route::get('/simulators', [Client\Student\SimulatorsController::class, 'index'])->name('simulators.index');
        Route::post('/simulators/{simulator}/start', [Client\Student\SimulatorsController::class, 'start'])->name('simulators.start');
    });
    
    // Партнер
    Route::prefix('partner')->middleware('role:partner')->name('partner.')->group(function () {
        Route::get('/dashboard', [Client\Partner\DashboardController::class, 'index'])->name('dashboard');
        Route::resource('cases', Client\Partner\CasesController::class);
        Route::post('/cases/{case}/archive', [Client\Partner\CasesController::class, 'archive'])->name('cases.archive');
        Route::get('/cases/{case}/applications', [Client\Partner\CasesController::class, 'applications'])->name('cases.applications');
        Route::post('/cases/{case}/applications/{application}/approve', [Client\Partner\CasesController::class, 'approveApplication'])->name('cases.applications.approve');
        Route::post('/cases/{case}/applications/{application}/reject', [Client\Partner\CasesController::class, 'rejectApplication'])->name('cases.applications.reject');
        Route::resource('teams', Client\Partner\TeamsController::class);
        Route::get('/profile', [Client\Partner\ProfileController::class, 'show'])->name('profile.show');
        Route::put('/profile', [Client\Partner\ProfileController::class, 'update'])->name('profile.update');
        Route::get('/analytics', [Client\Partner\AnalyticsController::class, 'index'])->name('analytics.index');
    });
    
    // Выход
    Route::post('/logout', [Auth\LoginController::class, 'logout'])->name('logout');
});
```

---

## 📊 База данных и модели

### Используемые модели (уже созданы):
- `User` - пользователи
- `StudentProfile` - профиль студента
- `PartnerProfile` - профиль партнера
- `TeacherProfile` - профиль преподавателя
- `CaseModel` - кейсы
- `CaseApplication` - заявки на кейсы
- `CaseTeamMember` - участники команды
- `Skill` - навыки
- `UserSkill` - навыки пользователя (pivot)
- `Badge` - бейджи
- `UserBadge` - бейджи пользователя (pivot)
- `Simulator` - симуляторы
- `SimulatorSession` - сессии симуляторов
- `AppNotification` - уведомления
- `ProgressLog` - логи прогресса

### Relationships (уже настроены в моделях):
- User -> StudentProfile/PartnerProfile/TeacherProfile (hasOne)
- User -> Skills (belongsToMany)
- User -> Badges (belongsToMany)
- User -> CaseApplications (hasMany как лидер)
- User -> CaseTeamMembers (hasMany)
- CaseModel -> Partner (belongsTo)
- CaseApplication -> CaseModel (belongsTo)
- CaseApplication -> Leader (User, belongsTo)
- CaseApplication -> TeamMembers (hasMany через CaseTeamMember)

---

## 🚀 Следующие шаги после реализации

1. **Тестирование:**
   - Unit тесты для контроллеров
   - Feature тесты для основных сценариев
   - E2E тесты для критичных путей

2. **Оптимизация:**
   - Кеширование статистики
   - Eager loading для отношений
   - Индексы в БД для частых запросов

3. **Дополнительные функции:**
   - Уведомления в реальном времени (WebSockets/Pusher)
   - Чат для команд
   - Email уведомления
   - Экспорт отчетов
   - Поиск по всему сайту (Elasticsearch опционально)

4. **UI/UX улучшения:**
   - Анимации переходов
   - Drag & drop для управления (если нужно)
   - Темная тема
   - Мобильная версия (responsive)

---

## 📝 Примечания

- Все контроллеры должны возвращать `Inertia::render()` вместо `view()`
- Использовать Form Requests для валидации вместо валидации в контроллере
- Все роуты должны быть защищены соответствующими middleware
- Flash сообщения передаются через session и автоматически доступны в `$page.props.flash` во всех Vue компонентах
- Ziggy используется для доступа к роутам в Vue: `route('admin.dashboard')`
- Pinia stores можно использовать для глобального состояния (например, корзина, фильтры)

---

**Файл создан:** {{ date }}
**Версия:** 1.0

